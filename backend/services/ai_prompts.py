from typing import Optional


# ============================================================
# AI PROMPTS — IslandQuiz
# ============================================================
#
# Общая идея:
# 1. Каждый тип вопроса имеет строгий контракт.
# 2. AI должен сначала продумать вопрос, затем проверить его.
# 3. JSON должен соответствовать формату frontend/backend.
# 4. Вопросы должны быть не просто "правильными", а хорошими
#    для реальной игры.
#
# Поддерживаемые типы:
# - choice
# - bool
# - text
# - matching
# - close
# - ordering
#
# ============================================================


TYPE_RULES = {
    "choice": """
ТИП: CHOICE — выбор одного правильного ответа.

СТРОГИЙ ФОРМАТ:
{
  "type": "choice",
  "question": "текст вопроса",
  "options": ["вариант A", "вариант B", "вариант C", "вариант D"],
  "correct": 0
}

ПРАВИЛА:
- Ровно 4 варианта ответа.
- Ровно один вариант должен быть правильным.
- "correct" — индекс правильного ответа: только 0, 1, 2 или 3.
- Все варианты должны отвечать на один и тот же вопрос.
- Неправильные варианты должны быть правдоподобными.
- Нельзя делать неправильные варианты очевидно нелепыми.
- Нельзя использовать "все вышеперечисленное".
- Нельзя использовать "ни один из перечисленных".
- Не делай правильный вариант заметно длиннее или подробнее остальных.
- Не делай правильный вариант грамматически единственным подходящим продолжением вопроса.
- Если вопрос требует числа, единицы измерения должны быть одинаковыми.
- Не используй несколько вариантов, которые можно считать одновременно правильными.
""",

    "bool": """
ТИП: BOOL — утверждение Да/Нет.

СТРОГИЙ ФОРМАТ:
{
  "type": "bool",
  "question": "утверждение",
  "correct": true
}

ПРАВИЛА:
- "question" должен быть именно утверждением, а не вопросом с вариантами.
- На утверждение должно однозначно отвечаться Да или Нет.
- "correct" должен быть только true или false.
- Утверждение должно быть фактически проверяемым.
- Не используй субъективные формулировки вроде "самый лучший", "самый интересный", "красивый".
- Избегай двусмысленности.
- Не делай утверждение истинным только из-за слишком узкой трактовки слов.
- Не используй двойное отрицание без необходимости.
""",

    "text": """
ТИП: TEXT — открытый вопрос с коротким ответом.

СТРОГИЙ ФОРМАТ:
{
  "type": "text",
  "question": "текст вопроса",
  "correctAnswer": "краткий ответ"
}

ПРАВИЛА:
- Ответ должен быть коротким: обычно 1–3 слова.
- Вопрос должен иметь однозначный ожидаемый ответ.
- Не задавай вопросы, на которые можно ответить длинным рассуждением.
- Не используй вопросы "почему", "объясните", "расскажите", если для ответа требуется развернутый текст.
- Если возможны несколько формулировок ответа, выбирай вопрос с однозначным ожидаемым ответом.
- "correctAnswer" должен содержать именно эталонный короткий ответ.
""",

    "matching": """
ТИП: MATCHING — сопоставление.

СТРОГИЙ ФОРМАТ:
{
  "type": "matching",
  "question": "Сопоставьте элементы:",
  "pairs": [
    {"left": "...", "right": "..."},
    {"left": "...", "right": "..."},
    {"left": "...", "right": "..."}
  ]
}

ПРАВИЛА:
- Ровно 3 пары, если количество не задано отдельно.
- Каждая пара должна иметь однозначное соответствие.
- Левая и правая части должны быть разного типа информации, если это делает задание понятнее.
- Не создавай пары, где один элемент логически подходит сразу к нескольким другим.
- Все пары должны относиться к теме.
- Не добавляй "options".
- Не добавляй "correct".
- Не добавляй "correctAnswer".
- Используй только поле "pairs" для соответствий.
""",

    "close": """
ТИП: CLOSE — заполнение пропусков.

СТРОГИЙ ФОРМАТ:
{
  "type": "close",
  "question": "Текст с ___ пропусками.",
  "correctAnswer": "ответ1|ответ2"
}

ПРАВИЛА:
- Используй символ ___ для каждого пропуска.
- Количество пропусков должно точно совпадать с количеством ответов в "correctAnswer".
- Ответы разделяй символом |.
- Каждый ответ должен соответствовать своему пропуску по порядку.
- Не используй дополнительные пропуски без ответа.
- Не используй ответы, которые можно вставить в несколько пропусков.
- Текст должен оставаться грамматически и смыслово понятным.
- Не делай слишком длинные пропуски, требующие целого предложения.
""",

    "ordering": """
ТИП: ORDERING — расстановка элементов в правильном порядке.

СТРОГИЙ ФОРМАТ:
{
  "type": "ordering",
  "question": "Расставьте элементы в правильном порядке:",
  "options": ["элемент 1", "элемент 2", "элемент 3", "элемент 4"]
}

ПРАВИЛА:
- "options" должен содержать минимум 3 элемента.
- Элементы должны иметь однозначный правильный порядок.
- Порядок в "options" должен быть УЖЕ правильным — от первого элемента к последнему.
- Не используй элементы, порядок которых зависит от субъективного мнения.
- Подходят хронологический, причинно-следственный, технологический, пространственный и другие объективные порядки.
- Не добавляй "correct".
- Не добавляй "correctAnswer".
- Не добавляй "pairs".
""",
}


def _difficulty_rules(difficulty: str) -> str:
    """
    Общие правила сложности.
    """

    difficulty = (difficulty or "mixed").lower()

    if difficulty == "easy":
        return """
СЛОЖНОСТЬ: EASY / ЛЁГКАЯ.

Вопрос должен проверять базовое знание темы.
- Ответ можно получить из одного хорошо известного факта или простой связи.
- Не требуй длинной цепочки рассуждений.
- Не используй намеренно редкие детали.
- Формулировка должна быть понятной ученику/игроку соответствующего уровня.
- Сложность должна быть лёгкой не из-за плохих вариантов ответа, а из-за доступности знания.
"""

    if difficulty == "medium":
        return """
СЛОЖНОСТЬ: MEDIUM / СРЕДНЯЯ.

Вопрос должен требовать уверенного знания темы.
- Допустимо сопоставление двух фактов.
- Допустимо простое применение знания.
- Неправильные ответы должны быть достаточно правдоподобными.
- Вопрос не должен решаться простым угадыванием по формулировке.
"""

    if difficulty == "hard":
        return """
СЛОЖНОСТЬ: HARD / СЛОЖНАЯ.

Вопрос должен действительно проверять глубокое понимание темы.
- Используй менее очевидные связи между фактами.
- Допустимо применение знания в новой ситуации.
- Допустимы детали, которые обычно знают хорошо подготовленные люди по теме.
- Вопрос не должен превращаться в бессмысленную энциклопедическую редкость.
- Сложность должна исходить из содержания, а не из запутанной формулировки.
- Не используй вопрос, ответ на который зависит от спорного или малоизвестного факта.
"""

    return """
СЛОЖНОСТЬ: MIXED / СМЕШАННАЯ.

Создавай вопросы с разной сложностью.

Для последовательности вопросов:
- начало — преимущественно easy;
- середина — medium;
- конец — medium/hard.

Не делай все вопросы одинаковыми по сложности.
Не подписывай сложность в тексте вопроса.
Поле "difficulty" должно соответствовать фактической сложности каждого вопроса.
"""

def _quality_rules() -> str:
    return """
ОБЩИЕ ПРАВИЛА КАЧЕСТВА:

1. СООТВЕТСТВИЕ ТЕМЕ
Каждый вопрос должен непосредственно относиться к указанной теме.
Не заполняй квиз общими вопросами, если пользователь указал конкретную тему.

2. ФАКТИЧЕСКАЯ КОРРЕКТНОСТЬ
Перед выдачей результата мысленно проверь каждый вопрос и правильный ответ.
Не придумывай факты, даты, имена, названия и статистику.

3. ОДНОЗНАЧНОСТЬ
У каждого вопроса должен быть однозначный правильный ответ.
Если два ответа могут считаться правильными, измени вопрос.

4. ИНТЕРЕСНОСТЬ
Не превращай квиз в список вопросов из Википедии.
Используй разные формы:
- факты;
- сравнение;
- узнавание;
- причинно-следственные связи;
- применение;
- интересные детали.

5. РАЗНООБРАЗИЕ
Не начинай каждый вопрос одинаково.
Не используй одну и ту же конструкцию вопроса подряд.

6. ПОВТОРЫ
Не повторяй один и тот же факт в разных вопросах.
Не задавай один вопрос несколько раз с небольшим изменением формулировки.

7. НИКАКИХ ПОДСКАЗОК В ФОРМУЛИРОВКЕ
Не вставляй правильный ответ в сам вопрос.
Не используй формулировки, которые явно подсказывают правильный вариант.

8. ПОЛЬЗОВАТЕЛЬСКИЕ ПОЖЕЛАНИЯ
Если пользователь указал пожелания, учитывай их, если они не противоречат корректности и формату.
Пожелания не должны нарушать структуру JSON.

9. ЯЗЫК
Отвечай на языке темы/пользовательского запроса.
Если тема на русском — вопросы на русском.
Если тема на английском — вопросы на английском.

10. ИГРОВОЙ КАЧЕСТВО
Вопрос должен быть интересен человеку, который реально играет в викторину.
Избегай скучных вопросов, если есть возможность сделать проверку знания интереснее.
"""


def _json_rules() -> str:
    return """
КРИТИЧЕСКИЕ ПРАВИЛА JSON:

- Верни только валидный JSON.
- Не используй Markdown.
- Не используй ```json.
- Не добавляй пояснения до или после JSON.
- Все строки должны быть корректно экранированы.
- Не добавляй комментарии внутрь JSON.
- Не добавляй поля, которых требует формат.
"""


def generate_question_prompt(
    topic: str,
    question_type: str,
    difficulty: str,
    wishes: str = "",
    count: int = 3,
) -> str:
    """
    Генерация нескольких независимых вариантов одного вопроса.
    Используется AI Helper.
    """

    question_type = question_type or "choice"
    type_rules = TYPE_RULES.get(question_type, TYPE_RULES["choice"])

    return f"""
Ты — профессиональный автор интеллектуальных викторин и редактор образовательного контента.

Твоя задача — создать {count} качественных вариантов вопроса по теме:
«{topic}»

ТИП ВОПРОСА:
{question_type}

{type_rules}

{_difficulty_rules(difficulty)}

{_quality_rules()}

ДОПОЛНИТЕЛЬНЫЕ ПОЖЕЛАНИЯ ПОЛЬЗОВАТЕЛЯ:
{wishes.strip() if wishes and wishes.strip() else "Нет дополнительных пожеланий."}

ВАЖНО:
- Каждый из {count} вариантов должен быть самостоятельным вопросом.
- Не делай варианты простыми перефразировками одного и того же вопроса.
- Если задана смешанная сложность, распределяй сложность между вариантами.
- Каждый объект должен содержать поле "difficulty":
  "easy", "medium" или "hard".

ФОРМАТ ОТВЕТА:

[
  {{
    "type": "{question_type}",
    "difficulty": "easy|medium|hard",
    ...
  }}
]

Для каждого объекта используй строго подходящий формат из правил выше.

{_json_rules()}
""".strip()


def improve_question_prompt(
    current_text: str,
    format_type: str,
    topic: str = "",
    wishes: str = "",
    difficulty: str = "mixed",
) -> str:
    """
    Улучшение существующего вопроса.
    Возвращает 3 варианта разной сложности.
    """

    normalized_format = format_type or "quiz-choice"

    format_to_type = {
        "quiz-choice": "choice",
        "quiz-bool": "bool",
        "quiz-text": "text",
        "quiz-matching": "matching",
        "quiz-close": "close",
        "quiz-ordering": "ordering",
        "choice": "choice",
        "bool": "bool",
        "text": "text",
        "matching": "matching",
        "close": "close",
        "ordering": "ordering",
    }

    question_type = format_to_type.get(normalized_format, "choice")
    type_rules = TYPE_RULES[question_type]

    return f"""
Ты — профессиональный редактор викторин.

Тебе дан существующий вопрос:

«{current_text}»

Тема:
«{topic.strip() if topic and topic.strip() else "не указана"}»

Тип:
{question_type}

{type_rules}

Твоя задача — создать 3 улучшенные версии этого вопроса.

ВЕРСИИ ДОЛЖНЫ ИМЕТЬ РАЗНУЮ СЛОЖНОСТЬ:

1. easy
2. medium
3. hard

При улучшении:
- сохраняй исходный смысл, если он корректен;
- исправляй неточности и двусмысленность;
- делай формулировку естественной;
- делай вопрос интереснее;
- не добавляй лишней воды;
- не меняй правильный ответ без необходимости;
- если исходный вопрос фактически некорректен, исправь его;
- соблюдай строгий формат выбранного типа.

Пожелания пользователя:
{wishes.strip() if wishes and wishes.strip() else "Нет дополнительных пожеланий."}

ОБЯЗАТЕЛЬНО:
- Каждый вариант должен быть полноценным объектом.
- Каждый объект должен содержать difficulty.
- Варианты должны реально отличаться по сложности, а не только по одному слову.
- Не добавляй пояснения.

ФОРМАТ:

{{
  "variants": [
    {{
      "type": "{question_type}",
      "difficulty": "easy",
      ...
    }},
    {{
      "type": "{question_type}",
      "difficulty": "medium",
      ...
    }},
    {{
      "type": "{question_type}",
      "difficulty": "hard",
      ...
    }}
  ]
}}

{_json_rules()}
""".strip()


def generate_quiz_prompt(
    topic: str,
    count: int = 10,
    wishes: str = "",
    difficulty: str = "mixed",
    type_distribution: Optional[dict] = None,
) -> str:
    """
    Генерация полного квиза.

    type_distribution можно будет использовать позже для ручного
    выбора количества типов вопросов.

    Например:
    {
        "choice": 5,
        "text": 2,
        "bool": 1,
        "matching": 1,
        "close": 1
    }

    Пока frontend может его не передавать.
    """

    if type_distribution:
        distribution_lines = []

        for qtype, amount in type_distribution.items():
            if qtype in TYPE_RULES and amount and amount > 0:
                distribution_lines.append(
                    f"- {qtype}: {amount} вопросов"
                )

        distribution_text = "\n".join(distribution_lines)

        if not distribution_text:
            distribution_text = """
- choice: 60%
- text: 20%
- bool: 10%
- matching: 10%
"""
    else:
        # Текущий стандартный режим IslandQuiz.
        # Для количества, не кратного 10, AI получает ориентир,
        # а не должен буквально считать проценты.
        distribution_text = """
СТАНДАРТНОЕ РАСПРЕДЕЛЕНИЕ:
- примерно 60% choice
- примерно 20% text
- примерно 10% bool
- примерно 10% matching

Типы close и ordering сейчас не включай в стандартное
распределение автоматически. Они поддерживаются системой,
но должны использоваться только когда явно запрошены.
"""

    type_contracts = "\n\n".join(
        f"### {name.upper()}\n{rules}"
        for name, rules in TYPE_RULES.items()
    )

    return f"""
Ты — профессиональный автор викторин, образовательных игр и интеллектуальных квизов.

Создай полноценный квиз на тему:

«{topic}»

Количество вопросов:
{count}

{_difficulty_rules(difficulty)}

{_quality_rules()}

РАСПРЕДЕЛЕНИЕ ТИПОВ:
{distribution_text}

ВАЖНО:
- Количество вопросов должно быть ровно {count}.
- Не создавай больше или меньше вопросов.
- Каждый вопрос должен иметь поле "type".
- Тип должен соответствовать одному из поддерживаемых типов.
- Формат каждого объекта должен строго соответствовать его типу.
- Не смешивай поля разных типов без необходимости.
- Не добавляй "options" к text/bool/matching/close.
- Не добавляй "pairs" к choice/text/bool/close/ordering.
- Не добавляй "correct" к text/matching/close/ordering.
- Не добавляй "correctAnswer" к bool/matching/ordering.
- Для choice "correct" — индекс от 0 до 3.
- Для bool "correct" — true или false.
- Для text "correctAnswer" — короткий ответ.
- Для matching используй "pairs".
- Для close используй "___" и ответы через |.
- Для ordering используй "options" уже в правильном порядке.

СТРУКТУРА ТИПОВ:

{type_contracts}

ТРЕБОВАНИЯ К ПОЛНОМУ КВИЗУ:

1. Каждый вопрос должен проверять отдельный факт или навык.
2. Не повторяй один и тот же факт.
3. Чередуй типы и формулировки.
4. Не делай первые вопросы всегда одинаковыми по структуре.
5. Если difficulty = mixed, сложность должна постепенно возрастать.
6. Если difficulty = easy, все вопросы должны быть лёгкими.
7. Если difficulty = medium, все вопросы должны быть средними.
8. Если difficulty = hard, все вопросы должны быть сложными.
9. Поле difficulty обязательно для каждого вопроса.
10. difficulty может быть только:
    "easy", "medium", "hard".
11. Название квиза должно быть коротким и естественным.
12. Название должно соответствовать теме.
13. Не называй квиз просто "Квиз по теме X", если можно придумать более интересное название.
14. Не используй кликбейт, если тема образовательная.
15. Не придумывай несуществующие факты.

ПРОВЕРКА ПЕРЕД ОТВЕТОМ:

Перед формированием JSON проверь каждый вопрос:

- Он действительно относится к теме?
- Есть ли однозначный правильный ответ?
- Правильный ли ответ указан?
- Соответствует ли структура выбранному типу?
- Нет ли повторения другого вопроса?
- Соответствует ли сложность заявленной сложности?
- Нет ли подсказки в формулировке?
- Нет ли двух правильных вариантов?
- Не нарушены ли правила конкретного типа?

Если вопрос не проходит проверку — исправь его до формирования JSON.

ДОПОЛНИТЕЛЬНЫЕ ПОЖЕЛАНИЯ:
{wishes.strip() if wishes and wishes.strip() else "Нет дополнительных пожеланий."}

ФОРМАТ ОТВЕТА:

{{
  "title": "Название квиза",
  "questions": [
    {{
      "type": "choice",
      "difficulty": "easy",
      "question": "Вопрос",
      "options": ["A", "B", "C", "D"],
      "correct": 0
    }},
    {{
      "type": "text",
      "difficulty": "medium",
      "question": "Вопрос",
      "correctAnswer": "Ответ"
    }},
    {{
      "type": "bool",
      "difficulty": "easy",
      "question": "Утверждение",
      "correct": true
    }},
    {{
      "type": "matching",
      "difficulty": "medium",
      "question": "Сопоставьте:",
      "pairs": [
        {{"left": "A", "right": "1"}},
        {{"left": "B", "right": "2"}},
        {{"left": "C", "right": "3"}}
      ]
    }},
    {{
      "type": "close",
      "difficulty": "medium",
      "question": "Текст с ___ и ___",
      "correctAnswer": "ответ1|ответ2"
    }},
    {{
      "type": "ordering",
      "difficulty": "hard",
      "question": "Расположите по порядку:",
      "options": ["Первый", "Второй", "Третий"]
    }}
  ]
}}

Показывай только те типы, которые реально нужны в соответствии
с распределением.

{_json_rules()}
""".strip()


def generate_jeopardy_categories_prompt(
    topic: str,
    wishes: str = "",
) -> str:
    """
    Генерация категорий Jeopardy.
    """

    return f"""
Ты — профессиональный автор игры «Своя игра» (Jeopardy).

Тема всей игры:
«{topic}»

Придумай 3 сильные категории для этой темы.

ТРЕБОВАНИЯ:

- Категории должны заметно отличаться друг от друга.
- Каждая категория должна охватывать отдельный аспект темы.
- Категория должна позволять создать вопросы от 100 до 500 очков.
- Не создавай слишком узкие категории, в которых невозможно придумать
  пять вопросов разной сложности.
- Не используй категории, которые фактически являются одной и той же темой.
- Названия должны быть короткими и естественными.
- Описание должно объяснять, какие вопросы будут внутри категории.

Пожелания:
{wishes.strip() if wishes and wishes.strip() else "Нет дополнительных пожеланий."}

ФОРМАТ:

{{
  "categories": [
    {{
      "name": "Название",
      "description": "Краткое описание"
    }},
    {{
      "name": "Название",
      "description": "Краткое описание"
    }},
    {{
      "name": "Название",
      "description": "Краткое описание"
    }}
  ]
}}

{_json_rules()}
""".strip()


def generate_jeopardy_questions_prompt(
    category: str,
    empty_slots: list[int],
    wishes: str = "",
) -> str:
    """
    Генерация вопросов Jeopardy для конкретной категории.
    """

    difficulty_map = {
        100: "очень лёгкий",
        200: "лёгкий",
        300: "средний",
        400: "сложный",
        500: "очень сложный",
    }

    slots_desc = "\n".join(
        f"- {slot} очков — {difficulty_map.get(slot, 'средний')} уровень"
        for slot in empty_slots
    )

    return f"""
Ты — профессиональный автор вопросов для игры «Своя игра» (Jeopardy).

КАТЕГОРИЯ:
«{category}»

Нужно создать вопросы для следующих пустых слотов:

{slots_desc}

ОСНОВНОЙ ПРИНЦИП:

Чем выше стоимость вопроса, тем сложнее должно быть получить ответ.

100:
- базовый факт;
- очевидное знание;
- вопрос должен быть доступен человеку, знакомому с темой.

200:
- обычное знание темы;
- немного менее очевидный факт.

300:
- уверенное знание;
- возможна связь двух фактов.

400:
- глубокое знание;
- менее очевидная информация;
- возможна комбинация нескольких знаний.

500:
- очень сложный вопрос для хорошо подготовленного игрока;
- редкий, но проверяемый факт;
- сложная связь или глубокое понимание темы.

ВАЖНО:
Сложность не должна достигаться бессмысленной редкостью.
Не используй сомнительные факты или информацию, которую невозможно
однозначно проверить.

ВОПРОСЫ:

- Каждый вопрос должен быть именно вопросом Jeopardy.
- Не используй варианты ответа.
- Ответ должен быть коротким и однозначным.
- Не повторяй факты между вопросами.
- Каждый вопрос должен строго соответствовать категории.
- Не задавай вопрос, ответ на который находится прямо в формулировке.
- Не делай 500-очковый вопрос просто вопросом о случайной дате или числе.

Пожелания:
{wishes.strip() if wishes and wishes.strip() else "Нет дополнительных пожеланий."}

ПРОВЕРКА:

Перед выдачей проверь:
- соответствует ли вопрос категории;
- соответствует ли сложность стоимости;
- является ли ответ фактически правильным;
- является ли ответ однозначным;
- нет ли повторов;
- нет ли подсказки в вопросе.

ФОРМАТ:

{{
  "questions": [
    {{
      "points": 100,
      "difficulty": "очень лёгкий",
      "q": "Вопрос?",
      "a": "Ответ"
    }}
  ]
}}

Создай только вопросы для указанных слотов.

{_json_rules()}
""".strip()